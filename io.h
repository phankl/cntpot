#ifndef io_header
#define io_header

#include <vector>
#include <iostream>

namespace io{
	void printVector(const std::vector<double>&);
	void printMatrix(const std::vector<std::vector<double>>&);
}

#endif
